{
  "name": "react-slideshow-main",
  "private": true,
  "scripts": {
    "docs:dev": "yarn workspace doc-site vite",
    "build": "yarn workspace react-slideshow build"
  },
  "workspaces": {
    "packages": [
      "packages/*",
      "sites/*"
    ]
  }
}
